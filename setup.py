from setuptools import setup, find_packages

setup(
    name="mome_data",
    version="0.1.0",
    description="Data utilities.",
    author="mOmE",
    packages=find_packages(),
    python_requires=">=3.6",
)
